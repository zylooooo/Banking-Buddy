# Banking Buddy Client Service - Environment Configuration
# Copy this file to .env.client-service and update with your actual values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
SERVER_PORT=8081

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_HOST=mysql-db
DB_USERNAME=root
DB_PASSWORD=BankingBuddy2025
DB_URL=jdbc:mysql://mysql-db:3306/crm_clients?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true

# =============================================================================
# AWS CONFIGURATION
# =============================================================================
AWS_REGION=ap-southeast-1
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# =============================================================================
# AUDIT LOGGING CONFIGURATION
# =============================================================================
AUDIT_SQS_QUEUE_URL=https://sqs.ap-southeast-1.amazonaws.com/YOUR_ACCOUNT/audit-logs
AUDIT_SOURCE_SERVICE=client-service
AUDIT_LOG_RETENTION_DAYS=30

# =============================================================================
# EMAIL CONFIGURATION (SES)
# =============================================================================
SES_SOURCE_EMAIL=noreply@bankingbuddy.com

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
SECURITY_ENABLED=false
MOCK_USER_ROLE=AGENT

# =============================================================================
# FLYWAY CONFIGURATION
# =============================================================================
FLYWAY_ENABLED=true

# =============================================================================
# HOW TO GET THESE VALUES
# =============================================================================
# 1. AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY:
#    - Use your AWS CLI credentials
#    - Or create IAM user with SQS and SES permissions
#
# 2. AUDIT_SQS_QUEUE_URL:
#    - Run: terraform output -json
#    - Look for audit_sqs_queue_url
#
# 3. SES_SOURCE_EMAIL:
#    - Verify this email in AWS SES console
#    - Or use a verified domain
#
# 4. MOCK_USER_ROLE:
#    - For development: AGENT (default)
#    - Options: AGENT, ADMIN, ROOT_ADMIN
#
# 5. For local testing without Docker:
#    - Change DB_URL to: jdbc:mysql://localhost:3306/crm_clients?createDatabaseIfNotExist=true
