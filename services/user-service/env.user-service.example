# Banking Buddy User Service - Environment Configuration
# Copy this file to .env.user-service and update with your actual values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_URL=jdbc:db_url_here
DB_USERNAME=db_username
DB_PASSWORD=db_password

# =============================================================================
# AWS CONFIGURATION
# =============================================================================
AWS_REGION=ap-southeast-1
COGNITO_USER_POOL_ID=ap-southeast-1_XXXXXXXXX
COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
CRM_DB_SECRET_NAME=crm-db-credentials

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
ROOT_ADMIN_EMAIL=admin@bankingbuddy.com
CORS_ALLOWED_ORIGINS=*

# =============================================================================
# SPRING PROFILES (Optional)
# =============================================================================
# SPRING_PROFILES_ACTIVE=hybrid

# =============================================================================
# HOW TO GET THESE VALUES
# =============================================================================
# 1. COGNITO_USER_POOL_ID and COGNITO_CLIENT_ID:
#    - Run: terraform output -json
#    - Look for cognito_user_pool_id and cognito_client_id
#
# 2. AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY:
#    - Use your AWS CLI credentials
#    - Or create IAM user with Cognito and SES permissions
#
# 3. ROOT_ADMIN_EMAIL:
#    - Use your email address for testing
#    - This will be the first admin user created
#
# 4. CORS_ALLOWED_ORIGINS:
#    - For development: * (allows all origins)
#    - For production: specific domains only
